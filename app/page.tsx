import GenerateButton from './GenerateButton';

export default function HomePage() {
  // Home -> HomePage doesn't matter
  return (
    <main>
      <GenerateButton />
      <h1>Welcome to the store</h1>
    </main>
  );
}
